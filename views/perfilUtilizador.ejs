<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Perfil do Utilizador</title>
  <style>
    /* Estilos CSS */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
    }

    h2 {
      color: #333;
    }

    .profile-container {
      display: flex;
      align-items: center;
    }

    .profile-info {
      display: flex;
      flex-direction: column;
    }

    .profile-name {
      font-size: 32px;
      font-weight: bold;
      margin-bottom: 10px;
      color: #4caf50;
    }

    .profile-email {
      font-size: 16px;
      margin-bottom: 5px;
    }

    .profile-dob {
      font-size: 16px;
      margin-bottom: 5px;
    }

    .profile-level {
      margin-top: 10px;
      display: flex;
      align-items: center;
    }

    .star {
      color: #FFD700;
      font-size: 40px;
      margin-right: 5px;
      position: relative;
    }

    .star span {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 24px;
      font-weight: bold;
      color: #4caf50;
    }

    button {
      background-color: #4caf50;
      color: #fff;
      border: none;
      padding: 10px 20px;
      font-size: 14px;
      cursor: pointer;
      border-radius: 4px;
      margin-top: 20px;
    }

    a {
      color: #fff;
      text-decoration: none;
    }

    button a {
      color: #fff;
    }
  </style>
</head>
<body>
  <h2>Perfil do Utilizador</h2>

  <form action="/api/perfilUtilizador/<%= perfil._id %>/uploadProfilePicture" enctype="multipart/form-data" method="POST">
    <span>Upload Profile Picture:</span>
    <input type="file" name="mypic" required/><br>
    <input type="submit" value="Submit"> 
  </form>

  <div class="profile-container">
    <div class="profile-picture">
      <img src="/uploads/<%= perfil.imagem %>" alt="Imagem de Perfil">
    </div>
    <div class="profile-info">
      <div class="profile-name">
        <%= utilizador.nome %>
      </div>
      <div class="profile-email">
        Email: <%= utilizador.email %>
      </div>
      <div class="profile-dob">
        Data de Nascimento: <%= utilizador.dataNascimento.toISOString().split('T')[0] %>
      </div>
      <div class="profile-level">
        <span class="star"></span>
        <span><%= perfil.nivel %></span>
      </div>
    </div>
  </div>
  <button><a href="/">Voltar</a></button>
</body>
</html>